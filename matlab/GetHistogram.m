%%% function computes gradient histograms given cell parameters
%%% matrix M, starting position at (r,c) and dimensions of cell is (h,w)
function h = GetHistogram(M, r, c, h, w)
h = zeros(1,1,9);
    for j = c:w
        for k = r:h
            
        end
    end
end